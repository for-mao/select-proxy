{
"server": "{{ server }}",
"server_port": {{ server_port }},
"local_address": "{{ local_address }}",
"local_port": {{ local_port }},
"password": "{{ password }}",
"timeout": {{ timeout }},
"method": "{{ method }}",
"fast_open": {% if fast_open %}true{% else %}false{% endif %},
"workers": {{ workers }}
}